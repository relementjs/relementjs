---
import type { tag__dom_T } from '../isomorphic/index.js'
import { fragment_ } from '../server/index.js'
export interface Props {
	dom:tag__dom_T<'server'>
}
const { $, dom } = Astro.props
---

<Fragment set:html={fragment_(...[$ ?? dom].flat(Infinity)).toString()}/>

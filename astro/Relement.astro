---
import { fragment_ } from '../isomorphic/index.js'
import type { tag__dom_T } from '../isomorphic/index.js'
export interface Props {
	dom:tag__dom_T<'server'>
}
const { _, dom } = Astro.props
---

<Fragment set:html={fragment_(...[_ ?? dom].flat(Infinity)).toString()}/>
